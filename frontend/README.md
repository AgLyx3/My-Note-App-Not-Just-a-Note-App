# Frontend / mobile app (placeholder)

This directory holds **planning-only** assets until a React Native (or native) app is scaffolded in-repo.

- Test plan for the mobile stream: `__tests__/README-TEST-PLAN.md`
- Release gates: `docs/handoff/TDD-TEST-GATES-CHECKLIST.md` (module F1)

Do not add a `package.json` here until the team picks the mobile stack; the gate checklist documents the intended commands.
